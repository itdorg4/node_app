name: matrix-build

on: workflow_dispatch

jobs:
    job:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                version: [16, 18, 20]
        steps:
            - name: Install Node
              uses: actions/setup-node@v6
              with:
                node-version: ${{ matrix.version}}
